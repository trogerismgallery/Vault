<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Download Secured | TROGERISM</title>
    <style>
        body { background-color: #050505; color: #ffffff; font-family: 'Helvetica Neue', Arial, sans-serif; text-align: center; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; margin: 0; text-transform: uppercase; letter-spacing: 2px; }
        .success-container { border: 1px solid #222; padding: 60px; background: #0c0c0c; max-width: 500px; }
        h1 { font-weight: 200; font-size: 1.5rem; margin-bottom: 30px; color: #fff; }
        .download-btn { display: inline-block; padding: 15px 40px; border: 1px solid #fff; color: #fff; text-decoration: none; font-size: 0.8rem; transition: 0.3s; margin-top: 20px; }
        .download-btn:hover { background: #fff; color: #000; }
        .note { color: #555; font-size: 0.6rem; margin-top: 40px; letter-spacing: 1px; }
    </style>
</head>
<body>
    <div class="success-container">
        <div style="color: #444; font-size: 0.7rem; margin-bottom: 10px;">Payment Verified</div>
        <h1>Vault Access Granted</h1>
        <p style="font-size: 0.7rem; color: #888; margin-bottom: 30px;">Your high-resolution digital asset is ready.</p>
        
        <a id="downloadLink" href="#" download class="download-btn">Download High-Res Image</a>
        
        <div class="note">A receipt has been sent to your email via PayPal.</div>
    </div>

    <script>
        // This script looks at the URL to see which sequence was bought
        const urlParams = new URLSearchParams(window.location.search);
        const item = urlParams.get('item');
        const downloadBtn = document.getElementById('downloadLink');

        if (item) {
            // Set the download link based on the sequence (v1_full.jpg, v2_full.jpg, etc.)
            downloadBtn.href = "v" + item + "_full.jpg";
            downloadBtn.innerText = "Download Sequence 0" + item;
        }
    </script>
</body>
</html>
